/**
 * レンダラーの `utils/assetsLayout.ts` と同一のセグメント名。
 * メインプロセスでは renderer を import しないためここに定数のみ置く。
 */
export const ASSETS_IMAGES_DIR = 'images'
export const ASSETS_DUST_DIR = 'dust'
export const ASSETS_REFERENCES_DIR = 'references'
